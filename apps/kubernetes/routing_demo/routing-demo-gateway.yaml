apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: routing-demo-gateway
  namespace: istio-enabled
spec:
  selector:
    istio: ingress
    app: istio-ingress
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
