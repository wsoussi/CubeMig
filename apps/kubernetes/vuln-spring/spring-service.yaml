apiVersion: v1
kind: Service
metadata:
  name: vuln-spring
spec:
  type: NodePort
  ports:
    - name: "http"
      port: 8080
      targetPort: 8080
      nodePort: 30081
  selector:
    app: vuln-spring
